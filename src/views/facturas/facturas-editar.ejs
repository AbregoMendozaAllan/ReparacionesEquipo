<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Factura</title>
</head>
<body>
    <h1>Editar Factura</h1>
    <form action="/facturas/edit/<%= factura.id_factura %>" method="POST">
        <label for="id_cliente">ID Cliente:</label>
        <input type="text" id="id_cliente" name="id_cliente" value="<%= factura.id_cliente %>" required>
        <br>
        <label for="id_empleado">ID Empleado:</label>
        <input type="text" id="id_empleado" name="id_empleado" value="<%= factura.id_empleado %>" required>
        <br>
        <label for="fecha_emision">Fecha Emisión:</label>
        <input type="date" id="fecha_emision" name="fecha_emision" value="<%= factura.fecha_emision %>" required>
        <br>
        <label for="total">Total:</label>
        <input type="number" id="total" name="total" value="<%= factura.total %>" required>
        <br>
        <label for="estado">Estado:</label>
        <select id="estado" name="estado">
            <option value="pendiente" <%= factura.estado === 'pendiente' ? 'selected' : '' %>>Pendiente</option>
            <option value="pagada" <%= factura.estado === 'pagada' ? 'selected' : '' %>>Pagada</option>
        </select>
        <br>
        <button type="submit">Actualizar</button>
    </form>
</body>
</html>
