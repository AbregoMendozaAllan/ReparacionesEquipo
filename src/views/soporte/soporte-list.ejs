<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Solicitudes de Soporte</title>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma.min.css">

    <style>
        .container {
            max-width: 900px;
            margin: auto;
            padding-top: 20px;
        }

        a {
            margin-right: 10px;
        }

        a img {
            vertical-align: middle;
            transition: transform 0.2s;
        }

        a:hover img {
            transform: scale(1.1);
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

        thead {
            background-color: #000100;
            color: white;
        }

        tr:nth-child(even) {
            background-color: #f5f5f5;
        }

        td, th {
            padding: 10px;
            text-align: center !important;
        }

        .buttons {
            margin-top: 20px;
        }
    </style>
</head>
<body>

<div class="container">
    <h1 class="title is-3">Solicitudes de Soporte</h1>

    <div class="buttons">
        <a class="button is-info" href="/dashboard">Regresar al Dashboard</a>
        <a class="button is-primary" href="/soporte/crear">Crear Solicitud</a>
    </div>

    <table class="table is-striped is-hoverable is-fullwidth">
        <thead>
        <tr>
            <th>Equipo</th>
            <th>Descripción del Problema</th>
            <th>Fecha de Solicitud</th>
            <th>Estado</th>
        </tr>
        </thead>
        <tbody>
        <% solicitudes.forEach(solicitud => { %>
            <tr>
                <td><%= solicitud.equipo %></td>
                <td><%= solicitud.descripcion_problema %></td>
                <td><%= new Date(solicitud.fecha_solicitud).toLocaleString() %></td>
                <td><%= solicitud.estado %></td>
            </tr>
        <% }) %>
        </tbody>
    </table>
</div>

</body>
</html>
