<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Detalle del Equipo</title>
  <link rel="stylesheet" href="/static/styles/styles.css">
</head>
<body>
  <h1>Detalle del Equipo</h1>
  <p><strong>ID:</strong> <%= equipo.id_equipo %></p>
  <p><strong>Tipo:</strong> <%= equipo.tipo %></p>
  <p><strong>Marca:</strong> <%= equipo.marca %></p>
  <p><strong>Modelo:</strong> <%= equipo.modelo %></p>
  <p><strong>Serie:</strong> <%= equipo.serie %></p>
  <p><strong>Estado:</strong> <%= equipo.estado %></p>
  <p><strong>ID de Usuario Asignado:</strong> <%= equipo.id_usuario_asignado %></p>
  <a href="/equipos/edit/<%= equipo.id_equipo %>">Editar</a>
  <form action="/equipos/delete/<%= equipo.id_equipo %>" method="POST" onsubmit="return confirm('¿Estás seguro de que deseas eliminar este equipo?');">
    <button type="submit">Eliminar</button>
  </form>
</body>
</html>