<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Listado de Equipos</title>
</head>
<body>
  <h1>Listado de Equipos</h1>
  <a href="/equipos/create">Crear nuevo equipo</a>
  <table>
    <thead>
      <tr>
        <th>id_equipo</th>
        <th>tipo</th>
        <th>marca</th>
        <th>modelo</th>
        <th>serie</th>
        <th>estado</th>
        <th>id_usuario_asignado</th>
        <th>Acciones</th>
      </tr>
    </thead>
    <tbody>
      <% equipos.forEach(equipo => { %>
        <tr>
          <td><%= equipo.id_equipo %></td>
          <td><%= equipo.tipo %></td>
          <td><%= equipo.marca %></td>
          <td><%= equipo.modelo %></td>
          <td><%= equipo.serie %></td>
          <td><%= equipo.estado %></td>
          <td><%= equipo.id_usuario_asignado %></td>
          <td>
            <a href="/equipos/edit/<%= equipo.id_equipo %>">Editar</a>
            <form action="/equipos/delete/<%= equipo.id_equipo %>" method="POST" onsubmit="return confirm('¿Estás seguro de que deseas eliminar este equipo?');">
              <button type="submit">Eliminar</button>
            </form>
          </td>
        </tr>
      <% }); %>
    </tbody>
  </table>
</body>
</html>